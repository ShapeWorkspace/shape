# Auto-generated by dtree - do not edit manually
# Worktree-specific port mappings and volume mounts

services:
  postgres:
    ports:
      - "5434:5432"

  minio:
    ports:
      - "9102:9000"
      - "9202:9001"

  app:
    volumes:
      - /Users/mo/Desktop/shape/../agent-paper-comments:/app
      # Keep node_modules inside container for performance.
      # macOS Docker volume mounts are slow for many small files.
      - node_modules:/app/node_modules
    ports:
      - "8082:8080"
      - "5175:5173"
    environment:
      SERVER_PORT: "8082"
      CLIENT_PORT: "5175"
      HOST: http://localhost:5175
      ALLOWED_ORIGINS: http://localhost:5175
      S3_AVATARS_PUBLIC_URL: http://localhost:9102

volumes:
  node_modules:
